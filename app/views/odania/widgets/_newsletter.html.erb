<div class="widget">
	<h4><%= t('Newsletter') %></h4>

	<form action="<%= current_site.newsletter_url %>" method="post">
		<%= current_site.newsletter_header %>

		<label for="ui_email"><%= t('odania.email') %></label> <input type="text" name="contact_list_contact[email]" id="ui_email"/><br/>
		<input type="submit" name="ui_submit" value="<%= t('odania.Sign up') %>" id="ui_submit"/>

		<p>
			<small>
				<%= t('odania.newsletter_not_give_away') %>
			</small>
		</p>
	</form>
</div>
