<% content_for(:admin_selection) do %>
	<%= form_tag(select_odania_menu_admin_odania_menu_odania_menu_items_path) do %>
		<%= collection_select(:menu, :id, Odania::Menu.order('title ASC'), :id, :title, selected: @odania_menu.id) %>
		<%= submit_tag 'Select' %>
	<% end %>
	<br/>
<% end %>
