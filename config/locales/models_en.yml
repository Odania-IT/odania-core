en:
  models:
    site:
      domain:
        description: 'There can be one preferred Subdomain per Domain. It will be targeted if a invalid domain is taken.'
      subdomain:
        description: 'The subdomain under the domain.'
